

for(ent in Dir.read("."))
{
    suff = ""
    pref = "[FILE]"
    if(ent.type == 4)
    {
        suff = "/"
        pref = "[DIR] "
    }
    st = File.stat(ent.name)
    sz = st.size
    println(pref, " ", sz, " ", ent.name, suff)
}